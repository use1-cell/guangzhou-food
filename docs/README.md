# 广州今日吃乜

这是一个准备部署到 GitHub Pages 的静态广州美食导航。

## GitHub Pages 发布方式

1. 把仓库推送到 GitHub。
2. 进入仓库 `Settings`。
3. 打开 `Pages`。
4. Source 选择 `Deploy from a branch`。
5. Branch 选择 `main`，文件夹选择 `/docs`。
6. 保存后等待 GitHub 生成公开网址。

网站入口是 `docs/index.html`。
